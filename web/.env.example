# Local: leave empty — Vite proxies /api → http://127.0.0.1:8000
# Production (Vercel env): your Modal ASGI URL, no trailing slash
# VITE_API_BASE=https://akaaafk--movie-persona-api-fastapi-app.modal.run

VITE_API_BASE=
