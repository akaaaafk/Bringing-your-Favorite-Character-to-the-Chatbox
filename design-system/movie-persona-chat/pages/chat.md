# Chat — overrides

- Character rail + conversation (Operate / product mode — not a teaching lab)
- Photo avatars with listening / speaking moods
- Tap-toggle mic → STT → auto-send → Best-of-N → TTS (TTS **muted by default**; header unmute + Stop)
- Viewport-locked shell (`h-dvh`): rail + scrollable thread + composer
- **Mobile:** avatar chip strip + collapsed Takes drawer; thread first. **Desktop:** full cast rail
- **Per-character threads** — switching cast keeps each conversation; Clear chat only wipes the active one; **sessionStorage** restores threads/drafts/settings after refresh or Cast back
- Takes settings as product preference (“Sharper replies”), not score pedagogy; no in-bubble research scores
- Preview badge when API/models offline
- Composer `text-base` on mobile; safe-area pad; visible focus rings on cast / stage / chips
